# Optional. Leave empty to use the same-PC local proxy at http://127.0.0.1:8787/api/analyze.
# For a Cloudflare Tunnel or other HTTPS proxy, set this in GitHub repo Variables as VITE_AI_PROXY_URL.
VITE_AI_PROXY_URL=
